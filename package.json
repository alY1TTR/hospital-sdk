{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.1.3",
    "lerna": "^6.6.2",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish"
  }
}
